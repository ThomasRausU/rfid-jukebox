DL_DIR = "${TOPDIR}/downloads"
SSTATE_DIR = "${TOPDIR}/sstate-cache"
TMPDIR = "${TOPDIR}/tmp"

DISTRO = "poky-jukebox"
MACHINE = "raspberrypi0-2w-64"

INHERIT += "rm_work"
RM_WORK_EXCLUDE += "jukebox-image"
